import { createServer } from 'node:http'; import { ReconciliationService } from './engine.js'; import { fixture } from './fixtures.js';
const svc=new ReconciliationService(); const send=(res:any,status:number,body:unknown)=>{res.writeHead(status,{'content-type':'application/json'});res.end(JSON.stringify(body));};
createServer((req:any,res:any)=>{const url=new URL(req.url,'http://localhost');const date=url.searchParams.get('date')||fixture.date;if(req.method==='GET'&&url.pathname==='/health')return send(res,200,{ok:true});if(req.method==='GET'&&url.pathname==='/api/state')return send(res,200,svc.list());if(req.method==='GET'&&url.pathname==='/api/reconciliation')return send(res,200,svc.reconcile(date));if(req.method==='GET'&&url.pathname==='/api/exceptions')return send(res,200,svc.list().exceptions.filter(x=>x.date===date));if(req.method==='GET'&&url.pathname==='/api/closes')return send(res,200,svc.list().closes);if(req.method==='GET'&&url.pathname==='/api/adjustments')return send(res,200,svc.list().adjustments);if(req.method==='POST'&&url.pathname==='/api/close')return send(res,200,svc.close(date));if(req.method==='POST'&&url.pathname==='/api/adjust')return send(res,200,svc.adjust());return send(res,404,{error:'not found'});}).listen(Number(process.env.PORT||3000),()=>console.log('Listening on http://localhost:3000'));
